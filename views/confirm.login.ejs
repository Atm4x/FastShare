<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Подтверждение входа</title>
    <link rel="stylesheet" href="/login-styles.css">
</head>
<body>
    <div class="login-container">
        <div class="login-box">
            <h1>Подтверждение входа</h1>
            <p>Нажмите кнопку ниже для подтверждения входа в систему</p>
            <button id="confirmButton">Подтвердить вход</button>
        </div>
    </div>

    <script>
        document.getElementById('confirmButton').addEventListener('click', function() {
            fetch('/confirm-login/<%= sessionId %>', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('Вход успешно подтвержден. Вы можете закрыть это окно.');
                } else {
                    alert('Произошла ошибка при подтверждении входа.');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Произошла ошибка при подтверждении входа.');
            });
        });
    </script>
</body>
</html>